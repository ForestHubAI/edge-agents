name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  go:
    name: Go (test + vet + build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: go
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v5
        with:
          go-version-file: go/go.mod
          cache-dependency-path: go/go.sum
      - run: go vet ./...
      - run: go build ./...
      - run: go test ./... -race

  ts:
    name: TS (typecheck + test + build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ts
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: ts/package-lock.json
      - run: npm ci
      - run: npm run typecheck
      - run: npm run build
      - run: npm test

  python:
    name: Python (lint + test + build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: py/onnx
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - run: pip install -r requirements.txt
      - run: pip install -e ".[dev]"
      - run: ruff check .
      - run: python -m pytest -q
      - run: python -m compileall app

  license-headers:
    name: License headers
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 20
      - run: node scripts/license-headers.mjs

  drift:
    name: Contract codegen drift
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v5
        with:
          go-version-file: go/go.mod
          cache-dependency-path: go/go.sum
      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: ts/package-lock.json
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Regenerate Go bindings
        working-directory: go
        run: go generate ./...
      - name: Check Go bindings in sync
        run: |
          if ! git diff --exit-code -- 'go/api/**/*.gen.go'; then
            echo "::error::Go bindings are out of sync with contract/. Run 'go generate ./...' in go/ and commit the result."
            exit 1
          fi
      - name: Install TS deps
        working-directory: ts
        run: npm ci
      - name: Regenerate TS bindings
        working-directory: ts
        run: npm run generate
      - name: Check TS bindings in sync
        run: |
          if ! git diff --exit-code -- 'ts/workflow-core/src/api/**'; then
            echo "::error::TS bindings are out of sync with contract/. Run 'npm run generate' in ts/ and commit the result."
            exit 1
          fi
      - name: Install Python codegen tool
        working-directory: py/onnx
        run: pip install -e ".[dev]"
      - name: Regenerate Python bindings
        working-directory: py/onnx
        run: datamodel-codegen
      - name: Check Python bindings in sync
        run: |
          if ! git diff --exit-code -- 'py/onnx/app/api/models.py'; then
            echo "::error::Python bindings are out of sync with contract/. Run 'datamodel-codegen' in py/onnx/ and commit the result."
            exit 1
          fi
